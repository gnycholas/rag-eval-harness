# Copy to .env. Retrieval needs none of this.
RAG_PROVIDER=stub
# GOOGLE_GENAI_API_KEY=
# ANTHROPIC_API_KEY=
# RAG_MODEL=
# RAG_JUDGE_MODEL=
# Requests per minute the client allows itself. The Gemini free tier gives 15
# per model, and going over earns a retry delay of most of a minute.
# RAG_RPM=15
